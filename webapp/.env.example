# SnapStash Configuration

# API Authentication
# Set a strong API key to protect backend endpoints from external access
# Frontend requests are automatically exempted (internal Docker network)
API_KEY=your-secret-api-key-change-me

# You can generate a secure key with:
# openssl rand -hex 32
